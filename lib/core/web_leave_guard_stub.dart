import 'package:flutter/widgets.dart';

void initWebLeaveGuard() {}

class WebLeaveGuard extends StatelessWidget {
  const WebLeaveGuard({super.key, required this.child});

  final Widget child;

  @override
  Widget build(BuildContext context) => child;
}
